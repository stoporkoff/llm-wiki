# Workspace Schema

The canonical page contract lives in
`.agents/skills/llm-wiki/references/page-schema.md`.

Add domain-specific page types, required relationships, naming rules, or review gates here. Keep
the shared skill generic and version local conventions with the workspace.
